import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Emtia Öngörü — Altın & Gümüş Analiz",
  description: "Altın ve gümüş için yapay zeka destekli kısa ve uzun vadeli öngörü paneli",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="tr">
      <body>{children}</body>
    </html>
  );
}
